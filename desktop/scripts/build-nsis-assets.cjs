/**
 * Placeholder for optional NSIS extras (installer sidebar/banner bitmaps, etc.).
 * electron-builder works with defaults in electron-builder.json; extend this script if you add custom assets under electron/.
 */
// eslint-disable-next-line no-console
console.log('build-nsis-assets: no extra assets required');
process.exit(0);
